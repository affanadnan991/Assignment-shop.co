// Deprecated: Product data is now served dynamically via Express REST API backend (server/index.js)
import { Product, Review } from '@/types';

export const MOCK_PRODUCTS: Product[] = [];
export const MOCK_REVIEWS: Review[] = [];
